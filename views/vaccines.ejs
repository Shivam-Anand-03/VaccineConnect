<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Available Vaccines - VaccineConnect</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        body {
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #e0f7fa, #f1f8e9);
            font-family: 'Poppins', sans-serif;
            color: #333;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            /* Set minimum height to allow for scrolling */
        }

        nav {
            width: 100%;
            background-color: #1a73e8;
            padding: 15px 0;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
            position: sticky;
            /* Makes the navbar sticky */
            top: 0;
            /* Keeps it at the top of the page */
            z-index: 1000;
            /* Ensures it stays above other content */
        }

        nav ul {
            display: flex;
            justify-content: center;
            list-style-type: none;
            margin: 0;
            padding: 0;
        }

        nav ul li {
            margin: 0 20px;
        }

        nav ul li a {
            color: white;
            text-decoration: none;
            font-size: 1.1em;
            transition: color 0.3s ease;
        }

        nav ul li a:hover {
            color: #ffcc00;
        }

        .container {
            text-align: center;
            max-width: 800px;
            width: 100%;
            background-color: #fff;
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0px 10px 25px rgba(0, 0, 0, 0.2);
            margin: 20px auto;
            /* Center the container and add margin */
            flex: 1;
            /* Allows the container to grow and fill space */
            overflow-y: auto;
            /* Adds vertical scrolling if needed */
        }

        h1 {
            color: #1a73e8;
            margin-bottom: 30px;
            font-size: 2.5em;
        }

        ul {
            list-style-type: none;
            padding: 0;
        }

        ul li {
            margin: 15px 0;
            font-size: 1.2em;
        }

        ul li a {
            text-decoration: none;
            color: #444;
            font-weight: bold;
            transition: color 0.3s ease;
        }

        ul li a:hover {
            color: #1a73e8;
        }

        .button-group {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            margin-top: 30px;
        }

        .button-group a,
        .button-group button {
            background-color: #1a73e8;
            color: white;
            padding: 12px 25px;
            border: none;
            border-radius: 10px;
            text-align: center;
            font-size: 1.1em;
            margin: 10px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            text-decoration: none;
        }

        .button-group a:hover,
        .button-group button:hover {
            background-color: #145ab6;
        }
    </style>
</head>

<body>
    <!-- Navigation Bar -->
    <nav>
        <ul>
            <li><a href="/vaccines">Home</a></li>
            <li><a href="/my-appointments">Appointments</a></li>
            <li><a href="/notifications">Notifications</a></li>
            <li><a href="/profile">Profile</a></li>
            <li><a href="/insurance">Insurance</a></li>
            <li><a href="/vaccine-info">Vaccine Awareness</a></li>
            <li><a href="/logout">Logout</a></li>
        </ul>
    </nav>

    <!-- Main Container -->
    <div class="container">
        <h1>Available Vaccines</h1>
        <ul>
            <% vaccines.forEach(vaccine=> { %>
                <li>
                    <a href="/vaccines/<%= vaccine.vaccine_id %>/hospitals">
                        <%= vaccine.vaccine_name %> - <%= vaccine.vaccine_type %>
                    </a>
                </li>
                <% }); %>
        </ul>
    </div>
</body>

</html>