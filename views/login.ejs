<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - VaccineConnect</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        /* Softer background gradient */
        body {
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(135deg, #f0f4ff, #d2f0e8);
            font-family: 'Poppins', sans-serif;
            color: #333;
        }

        /* Darkened form card */
        .login-container {
            background-color: #fff;
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0px 10px 25px rgba(0, 0, 0, 0.2);
            width: 400px;
            max-width: 90%;
            border: 2px solid rgba(0, 0, 0, 0.1);
        }

        h1 {
            text-align: center;
            font-size: 2.5em;
            margin-bottom: 30px;
            color: #1a73e8;
        }

        label {
            display: block;
            margin: 10px 0 5px;
            font-size: 1.1em;
            color: #444;
        }

        input {
            width: 100%;
            padding: 12px;
            margin-bottom: 20px;
            border-radius: 10px;
            border: 2px solid #ccc;
            font-size: 1em;
            background-color: #f9f9f9;
            color: #333;
        }

        input:focus {
            outline: none;
            border-color: #1a73e8;
            background-color: #fff;
        }

        button {
            width: 100%;
            padding: 12px;
            background-color: #1a73e8;
            color: #fff;
            font-size: 1.2em;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #145ab6;
        }

        p {
            text-align: center;
            font-size: 1.1em;
            color: #555;
        }

        a {
            color: #ff5722;
            text-decoration: none;
            font-weight: bold;
            transition: color 0.3s ease;
        }

        a:hover {
            color: #ff784e;
        }

        /* Warning message styling */
        .warning {
            color: red;
            text-align: center;
            margin-bottom: 20px;
            font-size: 1.1em;
        }

        /* Subtle form appearance animations */
        .login-container {
            animation: fadeIn 1.5s ease-in-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(30px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>

<body>
    <div class="login-container">
        <h1>Login</h1>

        <% if (typeof warning !=='undefined' ) { %>
            <p class="warning">
                <%= warning %>
            </p>
            <% } %>

                <form action="/login" method="POST">
                    <label for="username">Username:</label>
                    <input type="text" id="username" name="username" required>

                    <label for="password">Password:</label>
                    <input type="password" id="password" name="password" required>

                    <button type="submit">Login</button>
                </form>
                <p>Don't have an account? <a href="/register">Register here</a>.</p>
    </div>
</body>

</html>