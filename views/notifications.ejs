<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Notifications - VaccineConnect</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #e3f2fd, #fff);
            color: #333;
            overflow-x: hidden;
            height: 100vh;
            /* Set body height */
            overflow-y: auto;
            /* Allow vertical scrolling */
        }

        nav {
            width: 100%;
            background-color: #1a73e8;
            padding: 15px 0;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
        }

        nav ul {
            display: flex;
            justify-content: center;
            list-style-type: none;
            margin: 0;
            padding: 0;
        }

        nav ul li {
            margin: 0 20px;
        }

        nav ul li a {
            color: white;
            text-decoration: none;
            font-size: 1.1em;
            transition: color 0.3s ease;
        }

        nav ul li a:hover {
            color: #ffcc00;
        }

        h1 {
            color: #1a73e8;
            text-align: center;
            margin-top: 20px;
        }

        .container {
            max-width: 1200px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0px 10px 25px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th,
        td {
            padding: 12px;
            text-align: left;
            border: 1px solid #ccc;
        }

        th {
            background-color: #1a73e8;
            color: white;
            font-weight: bold;
        }

        .unread {
            background-color: #f8d7da;
            /* Light red for unread */
        }

        .delivered {
            background-color: #d4edda;
            /* Light green for delivered */
        }

        .no-notifications {
            text-align: center;
            font-size: 1.2em;
            margin-top: 20px;
            color: #555;
        }

        .button-container {
            display: flex;
            justify-content: space-between;
            margin-top: 30px;
        }

        button {
            background-color: #1a73e8;
            color: white;
            padding: 12px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            font-size: 1em;
            flex: 1;
            margin: 0 10px;
        }

        button:hover {
            background-color: #145ab6;
        }

        @media (max-width: 768px) {
            button {
                margin: 5px 0;
            }
        }
    </style>
</head>

<body>
    <!-- Navigation Bar -->
    <nav>
        <ul>
            <li><a href="/vaccines">Home</a></li>
            <li><a href="/my-appointments">Appointments</a></li>
            <li><a href="/notifications">Notifications</a></li>
            <li><a href="/profile">Profile</a></li>
            <li><a href="/insurance">Insurance</a></li>
            <li><a href="/vaccine-info">Vaccine Awareness</a></li>
            <li><a href="/logout">Logout</a></li>
        </ul>
    </nav>

    <!-- Main Container -->
    <div class="container">
        <h1>Your Notifications</h1>

        <!-- Check if there are notifications -->
        <% if (notifications.length> 0) { %>
            <table>
                <thead>
                    <tr>
                        <th>Message</th>
                        <th>Sent At</th>
                        <th>Status</th>
                    </tr>
                </thead>
                <tbody>
                    <% notifications.forEach(notification=> { %>
                        <tr class="<%= notification.status === 'pending' ? 'unread' : 'delivered' %>">
                            <td>
                                <%= notification.message %>
                            </td>
                            <td>
                                <%= new Date(notification.sent_at).toLocaleString() %>
                            </td>
                            <td>
                                <%= notification.status==='sent' ? 'Delivered' : 'Unread' %>
                            </td>
                        </tr>
                        <% }); %>
                </tbody>
            </table>
            <% } else { %>
                <p class="no-notifications">No notifications available.</p>
                <% } %>

    </div>
</body>

</html>